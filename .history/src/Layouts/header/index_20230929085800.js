import './header.scss';
import background from '../'
function Header() {
    return (
        <header className="container header">
           <img src={} />
        </header>
    );
}

export default Header;
