import './header.scss';
import background from '../../assets'
function Header() {
    return (
        <header className="container header">
           <img src={} />
        </header>
    );
}

export default Header;
